# get the list of data using items() method
for key, values in data.items():
    for i in values:
        print(key, " : ", i)

order_status_list = ["Preparing", "Awaiting Pickup", "Out for Delivery", "Delivered"]

csv_file = csv.DictReader(file)
for row in csv_file:
 print(row['key'])


# Read orders from orders.txt file using file content manager
checkout = open("Practice\Orders.txt", "r")
basket = checkout.readlines()
# Print out orders
for product in basket:
    print(product.replace("\n",""))